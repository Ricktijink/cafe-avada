// React
import React from 'react'

// Components

export default function ArticleRow({children}) {
    return (
        <section className="article-row">
            {children}
        </section>
    )
}
