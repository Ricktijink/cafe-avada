// React
import React from 'react'

// Assets
import heroIcon from '../images/hero-banner-logo.png'

export default function Hero() {
    return (
        <section className="hero-fixed">
            <img src={heroIcon} alt="Avada Café logo"/>
        </section>
    )
}
